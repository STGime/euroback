##############################################################################
# Eurobase Worker — Kubernetes Deployment
# River job queue worker. No HTTP service needed.
##############################################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: eurobase
  labels:
    app: worker
    component: jobs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
        component: jobs
    spec:
      initContainers:
        # Run River schema migrations before the worker starts.
        # Uses the same image — the river CLI is baked into the worker binary
        # via a small migrate subcommand, or we run it via the Go migrate API.
        - name: migrate
          image: rg.fr-par.scw.cloud/eurobase-app/worker:latest
          command: ["worker", "--migrate-only"]
          envFrom:
            - secretRef:
                name: eurobase-secrets
      containers:
        - name: worker
          image: rg.fr-par.scw.cloud/eurobase-app/worker:latest
          resources:
            requests:
              memory: "128Mi"
              cpu: "125m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          envFrom:
            - secretRef:
                name: eurobase-secrets
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: eurobase-config
                  key: environment
      imagePullSecrets:
        - name: scw-registry
